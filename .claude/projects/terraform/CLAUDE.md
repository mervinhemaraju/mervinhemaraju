# Terraform Project

## Skills

@~/.claude/skills/terraform-skill/SKILL.md

## Tooling

- Terraform (latest stable)
- State backend: remote (update to match your backend — S3, GCS, Terraform Cloud, etc.)

## Project Structure

- Modules live in `modules/`
- Environments in `environments/` or `envs/` (update to match your layout)
- Shared variables in `variables.tf`, outputs in `outputs.tf`
